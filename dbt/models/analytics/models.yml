version: 2

models:
  - name: dim_supplier
    columns:
      - name: supplier_id
        tests:
          - unique 
          - not_null


  - name: dim_item
    columns:
      - name: item_id
        tests:
          - unique
          - not_null
          

  - name: dim_date
    columns:
      - name: date_id
        tests:
          - unique
          - not_null

  - name: fact_sales
